# OpenAI API key for AI-assisted XML recovery (optional but recommended for malformed files)
OPENAI_API_KEY=your-api-key-here

# Model to use for XML extraction (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
